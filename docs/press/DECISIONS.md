# PRESS — decisions log (SUBUR Honey)

`approved` = the user said it. `assumed` = PRESS decided it under delegation and the user can
overturn it.

| # | Date | Decision | Status |
|---|---|---|---|
| D1 | 2026-08-20 | Round 1 ships **7 articles per language × 3 languages = 21 articles** | approved (user: "press subur honey project for each lang 7 articles") |
| D2 | 2026-08-20 | Length **800–1,000 words** per article, sitting at the top of the band so the round does not read thinner than the nine live articles | assumed (PRESS default) |
| D3 | 2026-08-20 | Topics are chosen from **each language's own demand**, not one topic set translated three ways. Where a topic genuinely has demand in more than one language it becomes an `ARTICLE_GROUPS` group and earns hreflang; otherwise it ships single-language | assumed |
| D4 | 2026-08-20 | **One lead image per article**, 4:3 WebP ≤ 110 KB, matching the site's existing article convention. Not hero + 2 inline + OG: this host collapses on request count, and the blog cards reuse the same file | assumed |
| D5 | 2026-08-20 | Round payload ceiling **2.5 MB** total added bytes | assumed |
| D6 | 2026-08-20 | `sitemap.xml.ts` is patched to enumerate **all** articles (it looped `ARTICLE_GROUPS` only, so a single-language article would have shipped unlisted) | assumed |
| D7 | 2026-08-20 | `SEO.astro` is patched to emit hreflang **only for locales present in `alternates`** when an explicit alternates map is passed. It previously fell back to a derived path and would have emitted 404 hreflang URLs for single-language articles | assumed |
| D8 | 2026-08-20 | No Search Console data in Stage 1 — the Cogny MCP free call quota for the period is exhausted. Demand evidence is live SERP/PAA/competitor research and is labelled as such; no invented search volumes | assumed, reported in GATE.md |
| D9 | 2026-08-20 | Images generated free via `pollinations` (the `zimage` provider returns 401 — expired token) | assumed |
| D10 | 2026-08-20 | Work lands on branch `press-round-1` off `master` @ `23452bc`. Rollback = `git checkout master` (nothing else was touched) | assumed |
| D11 | 2026-08-20 | Per-article fact sheets are **the candidate section in the language's demand file plus the article's map row**, not 21 duplicate files. Each candidate section already carries sourced evidence with URLs, the competitor gap and a per-topic risk list; copying that into a second file would have created two versions of the same facts | assumed |
| D12 | 2026-08-20 | `ARTICLE_FIGURES` is now keyed through `getAssetKey()` — the group's English slug where a group exists, the article's own slug otherwise. Without it every single-language article silently lost its lead image and its blog card fell back to a category icon | assumed |
| D13 | 2026-08-20 | Fixed a **pre-existing** failure this round exposed: `node scripts/qa/banned-terms.mjs --dir=dist` was red before the round. `scripts/ai/facts-sync.mjs` is a `.mjs` file, which the src-mode scan does not read, so it kept writing "traditional-use idiom of Middle Eastern honey blends" into `about.md`, `faq.md` and `AI-FACTS.yml` — all three live on suburhoney.com today. Rewritten without the banned positioning; both gate modes now pass | assumed |
| D14 | 2026-08-20 | hreflang is emitted only for a real cluster: a single-language article gets **no** hreflang annotation at all, and `x-default` is emitted only when the cluster has an English member. Annotating a lone page with a self-referencing hreflang, or naming a Malay page as the fallback for every other language, are both claims the round cannot support | assumed |
| D15 | 2026-08-20 | Images are 1200×900 (4:3). Pollinations' anonymous tier permits **one queued request per IP** and clamps resolution, so generation is strictly sequential with long waits, and each result is cover-cropped to exactly 4:3 — `scripts/qa/image-audit.mjs` fails any content image rendered at a ratio other than its own | assumed |
| D16 | 2026-08-20 | Each of the nine live articles swapped one `related` slug for a round-1 article, so every new page has a contextual inbound link and no live page lost its own inbound link. The related grid is `sm:grid-cols-2`, so the count stays at exactly two rather than growing to three | assumed |
| D17 | 2026-08-25 | **Round 2 starts.** Stage 0 re-probe found `scripts/ai/facts-sync.mjs` still hardcoding banned positioning (`tradisional`, `Timur Tengah`, `تقليدي`, `الشرق الأوسط`) into the Malay/Arabic `definition`/`category`/`audience` strings it writes to `AI-FACTS.yml` — D13's fix from round 1 only touched the hand-written `about.md`/`faq.md`/llms prose, not this generator. Confirmed `AI-FACTS.yml` itself never ships (only `site.definition`, the English key, feeds `about.md`/`faq.md`/`AGENTS.md` per `scripts/ai/build.mjs`), so this was dead-but-dormant, not a live violation. Rewritten to drop the banned wording (kept the same facts: traditional-use → "eaten as a daily food"). `check:ai` and `banned-terms.mjs` (both modes) confirmed green after | assumed |
| D18 | 2026-08-25 | Round 2 ships **6 articles per language × 3 languages = 18 articles**, on branch `press/round-2` off `master` @ `b13c44b` | approved (user instruction) |
| D19 | 2026-08-25 | Search Console still out of free-call quota (Cogny, confirmed 2026-08-25, resets 1 Sep) — same as round 1 (D8). Stage 1 again runs on live SERP/autocomplete/PAA research only, no volume figures anywhere in the map | assumed, reported in GATE.md |
| D20 | 2026-08-25 | Same image convention as round 1 continued: **one lead image per article**, 4:3 WebP ≤110KB, generated via `bin/imgen.py` (pollinations; zimage still 401/dead per fresh probe). Repo image inventory swept first — the only three real photographs (couples, black seed, pure honey) are already assigned to their round-1/pre-round-1 articles and are page-specific graphics elsewhere (benefits/retail/why-us heroes); nothing in the inventory fits an unclaimed round-2 topic, so round 2 is generation-first same as round 1 was | assumed |
| D21 | 2026-08-25 | Round 2 topic map finalized: 6 EN / 6 MS / 6 AR (18 total), one new cross-language group **G6** (Manuka vs local/SUBUR: `manuka-honey-vs-local-honey` en Round 1 + `madu-manuka-vs-madu-tempatan-malaysia` ms + `al-farq-bayn-asal-manuka-wal-asal-al-malizi` ar), sharing the Round-1 English Manuka hero image via `getAssetKey()` rather than generating two new images for the same comparison shot. Considered and rejected grouping `hadiah-madu-untuk-raya` (ms) with `hadiyat-al-asal-lil-eid` (ar) — same festival, but hamper-culture vs single-jar framing diverge enough that claiming one hreflang cluster would overstate how alike the two pages are. Full map, cannibalisation check against all 30 live keywords, and discarded/reserve list in `docs/press/round-2/ROUND-2-MAP.md` | assumed |
| D22 | 2026-08-25 | Stage 6 adversarial review (3 reviewers, one per language) found and fixed: **en1** image regenerated (first render showed whole nut chunks with no honey visible, contradicting the "ground into the blend" fact — caught before it shipped); **ar3** and **ar6** each had one invented specific claim (an unsourced "competitors use only 3-4 ingredient names" comparison, an unsourced "eaten within a week" gift-consumption claim) — both rewritten to keep the true underlying point without the invented specific; **ms3** was rewritten to break cross-language translation-mirroring with the English Manuka article (same list/table/FAQ order) — facts unchanged, structure and phrasing made independent; **ms4** had one invented customer-behaviour norm removed; **ms5** had the infant-safety warning inverted into an implied "safe for 1+" endorsement, corrected back to warning-only phrasing. One MS answer paragraph pushed to 71 words by the ms3 rewrite, trimmed back to band. All 18 articles re-verified clean after fixes | assumed |
| D23 | 2026-08-25 | **Round 3 starts.** Ships **6 articles per language × 3 languages = 18 articles**, off `master` @ `420b3e0` | approved (user instruction) |
| D24 | 2026-08-25 | Fixed a **pre-existing** failure Stage 0 exposed, before any content work: `npm run check:ai` was permanently red and it was **not** content drift. `scripts/ai/facts-sync.mjs --check` compared the committed `AI-FACTS.yml` against freshly generated output with a strict `!==`; the repo is developed on Windows with `core.autocrlf=true` and carries no `.gitattributes`, so git hands the file back with CRLF while the generator always writes LF. All 163 lines "differed", not one character of content did. The gate could never go green on any Windows checkout, and a round that trusted it would either have believed the facts file had drifted or "fixed" it by committing a whole-file diff that could hide a real drift inside it. Comparison now normalises line endings on both sides; `check:ai` exits 0 | assumed |
| D25 | 2026-08-25 | **Search Console is available for the first time on this project** — the OAuth write token at `E:\TechVisionEra\gsc-token-write.json` is `siteOwner` on `sc-domain:suburhoney.com` (the URL-prefix form `https://suburhoney.com/` returns 403; the domain property works). This supersedes D8 and D19. suburhoney.com's own data is effectively empty — **zero query rows** over 16 months, one page with 4 clicks — so there is no "impressions but poor position" harvest and round 3 stays demand-led from SERP research. The usable contribution is `sc-domain:madinah.com.my`, the same retailer and audience: **222 honey-related queries** with real impressions and positions, captured in `docs/press/round-3/gsc-demand.md` and labelled throughout as a proxy for the audience, never as evidence about the SUBUR catalogue | assumed |
| D26 | 2026-08-25 | **Jar size, net weight, weight tolerance and jar duration are off limits for the whole round — both to assert and to deny.** `src/content/llms/{en,ms,ar}.txt` publish "250g, 400g and 500g jars, weight tolerance ±5% printed on every label" in all three languages and repeat it in their FAQs, and two launch articles (`en/honey-for-couples-malaysia`, `ms/madu-suami-isteri`) print "250g" plus a "lasts three to four weeks" duration. None of it exists in `src/config/site.ts`, the declared single source of truth, nor in the generated `AI-FACTS.yml`; and round 2's own `FACTS-COMMON.md` lists "jar size in ml/g" among the facts that do not exist and whose invention fails a round. The site's writers' brief therefore forbids exactly what the site's machine-readable brief publishes. Which one is wrong is an **owner** determination — either the label really carries those figures, in which case they belong in `site.ts` so every surface imports them, or they are residue from before the fabricated-data cleanup and must come out of three llms files and two live articles. Round 3 neither repeats nor contradicts the claim, and nothing was rewritten unilaterally: deleting a possibly-true published fact is as much a decision as inventing one | assumed, escalated to the owner in GATE.md |
| D27 | 2026-08-25 | Image convention unchanged from rounds 1–2 (one 4:3 lead per article, ≤110 KB WebP), and round 3 is **generation-first again for all 18 slots**, recorded honestly rather than dressed up as reuse. Stage 4a swept the whole repo: the only three real photographs (`article-couples`, `article-black-seed`, `article-pure-honey`) are already heroes of live articles, the four jar shots are 400 px wide and cannot honestly fill a 4:3 card without upscaling, and the `page-*`/`hero-bg-*`/`ritual-bg-*` files are money-page heroes and locale background plates, not subjects. There is no fourth distinct subject in the repo. **A photo shoot is the only thing that changes this for round 4** | assumed |
| D28 | 2026-08-25 | Round payload ceiling **2.2 MB** (down from round 1's 2.5 MB), measured in GATE.md. Images are **886×665**, not 1200×900: two real control generations at `-W 1200 -H 900` and `-W 1600 -H 1200` both returned 886×665, so that is the free provider's hard cap and `articleFigures.ts` declares the real size. `zimage` returned 401 for the third round running | assumed |
| D29 | 2026-08-25 | Round-3 map finalised: 6 EN / 6 MS / 6 AR. **Six hreflang groups**, more than either earlier round, and each one had to clear the same bar — *both sides arrived at the same core question independently, from their own demand research, without seeing the other's file*. **G7** en `what-is-sidr-honey` + ar `al-farq-bayn-asal-al-sidr-wal-asal-al-malizi`; **G8** en `why-is-honey-dark-or-light` + ms `kenapa-madu-jadi-gelap`; **G12** en `how-to-read-a-honey-label-malaysia` + ar `qiraat-mulsaq-al-asal-fi-malizia`. Three are retroactive, pairing a new article with a live one: **G9** ms `tarikh-luput-madu` + ar `hal-lil-asal-tarikh-salahiya`, **G10** ms `madu-untuk-ibu-mengandung` + en `is-honey-safe-during-pregnancy`, **G11** ms `madu-saffron` + ar `asal-bil-zafaran`. G9 and G11 have no English member and therefore get no `x-default`, per D14. Three refusals recorded as decisions in their own right: ar5 is **deliberately ungrouped** from the live en `honey-with-nuts-malaysia` (Arabic demand is DIY-shaped, English is product-category); ms3 `madu-khalta` is not grouped with the live ar ingredients article (category literacy vs reading SUBUR's own label); en1 ships single-language because Malay already owns verification through `cara-kenal-madu-asli` and Arabic discarded the equivalent cluster | assumed |
| D30 | 2026-08-25 | **Search Console produced a hole, not a keyword, and that is the round's best finding.** `yemeni honey` (1,935 impressions, position 11.5) and `yemeni sidr honey` (1,801, position 14.4) are the largest measured cluster this audience produces; grepping all sixteen live Arabic articles for `السدر`/`اليمني`/`يمني` returned **zero occurrences**. The biggest thing the audience asks has no page in any language. That is the origin of en2 and ar1 — the only topic in three rounds to arrive from measurement rather than SERP inspection. Two clusters were refused rather than dropped silently and are itemised in the demand files: ~**1,700 impressions** of sexual/aphrodisiac/marriage-outcome demand (including `aphrodisiac honey near me` at position **1.3**), which the food rules forbid this site from answering, and **every price query** (`black seed honey price` 574 imp), which the site cannot answer because it publishes no price | assumed |
| D31 | 2026-08-25 | **Every externally checkable number is verified once, centrally, in `docs/press/round-3/FACTS-VERIFIED.md`, and writers quote from there rather than from a demand file** — round 1's lesson that a research file is not a source. Verification sharpened two figures the English demand file had rounded: the 90%-fake claim is **Dr Zulkifli Mustafa of USM's Neuroscience Department** via Berita Harian, and the Health Ministry's counter is **45 of 769 samples = 5.85%**, tested against **Regulation 130 of the Food Regulations 1985** (≥60% reducing sugars, ≤10% sucrose, ≤80 mg/kg HMF) — not the "~6%" the demand file carried. A **narrow, deliberate exception** to the round's statute-citation ban is granted for Regulation 130 in the en1 family only: it is a composition standard and what MOH actually said it tested against, so naming it is reporting. Sub-regulation 18(6), penalties, and any implication that SUBUR was tested or would pass remain banned everywhere. Also verified and fenced: sidr botany (no bloom-duration figure — sources contradict outright; no price; no grading claim), the kelulut/tualang naming logic (**tualang is named for the tree the hive sits in, not the nectar** — it is multifloral jungle honey), and Malaysian label law (descending order by weight; tree nuts are a declared allergen, which makes SUBUR naming its walnut and almond a checkable trust signal) | assumed |
| D32 | 2026-08-25 | **A live inverted-physics finding was reported twice and checked, not acted on as reported.** Two Arabic writers independently flagged `src/content/articles/ar/al-ma-al-sakhin-wal-asal.ts` for saying nut particles "settle at the bottom", reading it as the inverted-density defect round 1 corrected across three languages. **It was a false positive.** The sentence sits in a table about what happens **in a cup of tea**, not in the jar — and once the honey disperses into water, ground nut kernels (~1.0 g/cm³) genuinely do sink. The float rule is nuts and comb against **honey** at ~1.4, which is a different comparison. Nothing was "fixed" into being wrong. But two independent readers hitting the same sentence is evidence the wording is genuinely ambiguous next to the site's own float article, so the cell was clarified to say the particles settle **to the bottom of the cup, after the honey dissolves into the water** — same meaning, no longer misreadable. Recorded here so round 4 does not rediscover it and "correct" a correct sentence | assumed |
| D33 | 2026-08-25 | **Image generation missed 8 of 14 — worse than this provider's usual one-in-three — and the failure was the prompt, not luck.** Two reproducible modes. (a) The style suffix "photographic still life, natural daylight" produced soft, painterly, out-of-focus frames: a corked bottle with no label where a shop shelf was asked for, two jars of greenish liquid with a plant growing out of one where kelulut and tualang were asked for, a ceramic pot with no saffron and no honey in it. (b) **Asking for a graded range never once worked** — "five jars from light gold to near black" returned five identical jars, "three jars pale/amber/almost black" returned three identical jars. The generator will not hold a gradient across repeated objects; **two objects with one stated contrast does work.** Regeneration pass therefore uses crisp product-photography language ("everything in sharp focus edge to edge, clean studio softbox lighting") and never more than two objects that must differ. Worth carrying into `bin/imgen.py`'s guidance for every future round | assumed |
| D34 | 2026-08-25 | **`figureAlt` is written before the image exists, so a reconciliation pass is mandatory, not optional.** Writers were briefed to describe the frame rather than their argument, and they did — but they could not see a file that had not been generated. Where the writers' frame is better, the **image** was regenerated to match: both authors of the sidr pair and both authors of the dark-vs-light pair independently chose "two jars side by side", which is a better comparison frame than the single jar and the three-jar gradient originally prompted. Where the writers' frame **cannot be generated honestly**, the **alt text** was rewritten: two articles asked for a legible ingredient list and allergen line, and one for honey weighed in grams on a kitchen scale — a generator garbles text and numbers, so an alt describing readable text on an image that has none is exactly the "alt describing an image the page is not showing" defect. Neither direction is a default; the test is which of the two can be made true | assumed |
| D35 | 2026-08-25 | **Adversarial review found 14 blockers across three languages, and almost none were bad writing.** They were the new articles disagreeing with the site's own published copy — the pattern this round was explicitly briefed to hunt. The four that matter most are logged separately below | assumed |
| D36 | 2026-08-25 | **A wrong number that would have been the round's most-quoted line.** `en/baking-with-honey-malaysia` shipped "about 80 g of honey per 100 g of sugar" — a **volume** ratio (¾ cup honey per 1 cup sugar ≈ 0.81) carried across into **mass** unchanged. Verified independently: 1 US cup honey = **340 g**, 1 US cup granulated sugar = **200 g**, so the real mass ratio is **≈ 128 g per 100 g**. The volume and mass ratios point in opposite directions and the error's size is exactly the honey-to-sugar density ratio. Two things make it instructive: the article's **own warning callout** described the mistake ("a cup of honey and a cup of sugar do not weigh the same") three blocks below where it made it; and its stated justification, "honey is sweeter spoon for spoon", is a **volume** observation used to license a **weight** reduction — honey is ~17% water and by weight is not sweeter than sugar. **Root cause: that article was the only one in the round whose entire spine had no entry in `FACTS-VERIFIED.md`**, because no baking section was ever written. One now exists, with the cup masses and the division written out, and a standing rule: **a ratio taken from a source in cups may never be published in grams without the cup masses and the arithmetic shown** | assumed |
| D37 | 2026-08-25 | **New failure mode named: fabrication by DENIAL.** `ar/hadiya-min-malizia-lil-ahl` invented nothing — it **denied** three things the site publishes: a bulk pathway ("no published route for large orders"), shipping abroad ("no service to send it to another country"), and the number of purchase channels (it wrote three; `llms/ar.txt` says "four purchase routes, and no fifth"). All three verified false: `src/components/sections/Wholesale.astro` renders a **wholesale enquiry form on every locale's homepage**, `/{lang}/contact/` and `/{lang}/retail/` both point at it, `llms/{en,ms,ar}.txt` carry a wholesale section plus an FAQ answering **yes**, and `/{lang}/retail/` publishes that delivery zones outside Malaysia are managed on the store's Shopee and TikTok Shop. **Root cause was this round's own facts brief**, which inherited "gift-box/bulk-order service" from round 2's list of things that do not exist. What actually does not exist is a published **price, MOQ or lead time**. `FACTS-COMMON.md` corrected with the evidence; the same false denial was also fixed on the **live** round-2 article `ar/hadiyat-al-asal-lil-eid`, which had shipped with it, along with a "three channels" count in the same file. **A brief that lists a fact as non-existent will produce a confident denial, and a denial is as false as an invention** | assumed |
| D38 | 2026-08-25 | **Both G8 articles called natural darkening a defect, contradicting the site's own machine brief.** `en/why-is-honey-dark-or-light` and `ms/kenapa-madu-jadi-gelap` both modelled colour as "nectar, added ingredients, heat — and only heat is a fault", explicitly ruling out **time**. `llms/ms.txt` states in **four** places that the blend "menjadi lebih gelap ... dari masa ke masa" and lists it beside crystallising and separating as normal raw-honey behaviour; the live `ms/cara-kenal-madu-asli` says raw honey "menghablur, berasing dan menjadi gelap dengan masa". The Malay article's callout even dismissed any page saying honey darkens over weeks as "selling something" — **while linking to that page**. Fixed on both sides by adding slow natural darkening as a fourth, non-fault cause and reframing the conclusion to **fast darkening = heat, slow darkening = normal**, which is what the articles' own first FAQ already said | assumed |
| D39 | 2026-08-25 | **Two round-3 articles were translations of their partners, and two contradicted them — both are hreflang-group defects, not writing defects.** (a) `ms/tarikh-luput-madu` mirrored the live Arabic `hal-lil-asal-tarikh-salahiya`: 8 FAQs vs 8, 7 H2s vs 7, the answer block closing on the same sentence in the same position, **three FAQ answers clause-for-clause**, and the body analogy swapped (salt-and-sugar → rice-and-salt) in the same slot. Proved a round-3 defect rather than house style by a control: the live Malay/Arabic hot-water pair shares an H2 skeleton but has genuinely different FAQ sets, and **no live pair has sentence-for-sentence answers**. (b) `ms/madu-untuk-ibu-mengandung` contradicted its own declared alternate — the live English says "**Yes**, and the NHS, APA and RCOG agree", the Malay said the site "does not make that determination, **and no food seller should**", which also implicitly accuses its own alternate of impropriety. **That one came from an over-tight instruction in this round's brief**: the ceiling forbids a *product* health claim but explicitly permits *reporting what public-health bodies say*. Fixed on the Malay side. The same article also claimed "no Malaysian page answers this question" while its own English partner has answered it since round 2, and attributed an unpublished "unpasteurised" product claim to a live article that never says it | assumed |
| D40 | 2026-08-25 | **Three reported findings were checked and dismissed rather than actioned — each would have made the site worse.** (i) Two Arabic writers flagged nut particles "settling" in a live article; verified correct because the sentence is about a **cup of tea**, not the jar (see D32). (ii) `scripts/qa/image-audit.mjs` reported two homepage background plates as "did not load"; both serve **200 with byte-identical size** and round 3 never touched them — an audit artefact on deferred images at a 390 px viewport. (iii) The Arabic fixer flagged the live `ar/shiraa-al-asal-online-fi-malizia` for saying "three channels"; that article is scoped to buying **online**, its three are Shopee, TikTok Shop and WhatsApp-for-enquiry, and it never calls WhatsApp a purchase channel — a deliberately scoped count, not the site's four-channel fact. **Verify before acting: a round that "fixes" a correct sentence has damaged the site to satisfy its own report** | assumed |
| D41 | 2026-08-25 | **Every fix round pushed articles out of the word band, and that is a flow defect rather than an agent one.** Adversarial fixes *add* words — a warm-water carve-out, a fourth cause of colour, a corrected FAQ, a replaced denial — and the round was deliberately written at the **top** of its band, so there was no headroom. Three regressions appeared on the first re-gate (two word counts, one 160-char meta description) and two more on the second (1625 and 1605 words, plus a 161-char description), each needing another trim pass. **The fix instruction must carry the band and the current count**, so a fixer trims as it edits instead of a separate pass chasing a moving target afterwards. Related: never re-gate while fixer agents are still writing — the first re-run and the second reported *different* files, because the tree was changing underneath. Re-gate only after every writer has reported | assumed |
| D42 | 2026-08-26 | **Round 4 ships 13 articles, not 18, and that is the finding rather than a shortfall.** Every researcher was told explicitly that a short honest list beats six padded ones and asked to say so if fewer cleared the bar. **All three did, independently** — English 5, Malay 3 plus a conditional, Arabic 4. Against 66 live articles, cannibalisation now has to clear the live **secondary** keyword arrays as well as the focus keywords, and that killed most candidates. Padding to 18 would have meant publishing six pieces that cannibalise live ones — the exact failure the flow exists to prevent | assumed |
| D43 | 2026-08-26 | **The cross-language consistency reviewer is a new, permanent role, and it justified itself on its first run.** Round 3 ran one adversarial reviewer per language, each scoped to its own corpus, so a new article in one language contradicting a live article in another was **structurally invisible to all three** — which is how the 90%-claim contradiction shipped. The new reviewer found three blockers no per-language reviewer could have seen: (i) the live English pregnancy article issued a safety **verdict** ("Yes", a yes/no table, "Honey stays on the table during pregnancy") while both its hreflang partners and **all three** machine briefs say the site never clears a food — and round 4's own new infant article, which links to it, says "this article clears no food for any child"; (ii) a **second** live Malay article still carried the round-3-class promise "we do not repeat that figure, because its source cannot be verified", thirty lines from the one round 4 had just fixed, while this round published an article naming that source; (iii) `llms/ar.txt` line 9 made crystallisation proof of authenticity, contradicting `en.txt`, `ms.txt` **and its own lines 102 and 252**. Per-language review covers everything inside a language; only a dedicated pass covers the seam | assumed |
| D44 | 2026-08-26 | **Three rounds of adversarial review never looked at `src/content/pages/`.** Reviewers were always pointed at articles and `llms*.txt`, so the money pages went unexamined — and that is where round 1's crystallisation correction, recorded as "applied to all 9 surfaces", had quietly failed. **Four more instances survived**: `benefits.ts` in all three languages asserting crystallisation "is a sign of a genuine raw blend", plus a Malay `why-us` table cell reading "Dijangka - tanda madu tulen". Found only because a new round-4 article links to `/en/benefits/` twice and would have sent readers straight to the sentence it had just refuted. All four corrected to the settled position. **Future reviewer briefs must name `src/content/pages/` and `src/components/` explicitly** — a claim can live on a money page for four rounds precisely because nobody is assigned to read it | assumed |
| D45 | 2026-08-26 | **A source-of-truth file can be internally inconsistent, and everything downstream inherits it silently.** `round-3/FACTS-VERIFIED.md` stated the baking rule as **¾ cup**, computed `0.75 × 340 = 255 g`, and then asserted **"The volume ratio is 0.81"** — a stale figure from the "¾ cup + 1 tbsp" reading abandoned mid-round-3. The Malay round-4 article published 0.81 faithfully, next to its own correct 0.75 derivation, on the one page whose whole premise is that competing conversion tables contradict themselves. Corrected in the facts file and the article. **The rule the file already carried was right and still did not save it: a ratio taken in cups may never be published in grams without the cup masses and the division written out. What was missing was checking the file against itself** | assumed |
| D46 | 2026-08-26 | **Round 4's writers refused more than round 3's, and the refusals were the round's best work.** en1 published **no numeric threshold** from Regulation 130A because only the categories were verified, and made that refusal visible on the page. en2 **fetched and read the Food Regulations 1985 text** to close a fabrication gate, and found the article's best argument — Malaysia defines "raw" for milk, cream and fruit and never for honey. It then **declined to print any threshold** after spotting an apparent mismatch with a live article, which I checked and cleared (Regulation 130's ≥60% reducing sugars / ≤10% sucrose / ≤80 mg/kg HMF are correct; the consolidated PDF it read predates an amendment). en3 listed **ten** places it declined to state a rule. ms4 cut a claim that separation proves the labelled solids are present, because it would smuggle a purity test back in. ar4 refused every statute number and said so in its own voice. **A brief that names the specific thing to refuse gets refusals; a brief that says "be careful" gets carelessness** | assumed |
| D47 | 2026-08-26 | **Round 5 starts.** Ships **6 articles per language × 3 languages = 18 articles**, single-session run (no subagent researchers/writers/reviewers available), off `main` @ `173a07d` (the image-reuse round). Topics chosen from real demand + real health/nutrition sources rather than SERP tooling — no keyword API or GSC quota available this session, same constraint as every prior round | approved (user instruction) |
| D48 | 2026-08-26 | **Six topics, all six shipped as three-language groups (G47–G52)**: honey and diabetes, honey for cough/cold, calories and sugar content, honey vs white sugar, safe daily amount, honey at suhoor/Ramadan. Every focus keyword checked against all 79 live articles' `primaryKeyword`/`keywords[]` arrays and against the other 17 articles in this round — no collision. `honey-vs-sugar-which-is-healthier` verified distinct from the live `honey-vs-gula-melaka` (white sugar vs. palm sugar, different intent). Full map in `ROUND-5-MAP.md` | assumed |
| D49 | 2026-08-26 | **Refused: honey pricing and weight-loss framing.** No defensible RM figure exists for "how much does honey cost" without inventing one, and SUBUR publishes no price; dropped rather than filled, per round-3 D30 precedent. Every diabetes/calorie source reviewed treats honey as calorically equivalent to sugar, so a weight-loss angle would require inventing a benefit no source supports — G49/G50 answer the underlying calorie question honestly instead | assumed |
| D50 | 2026-08-26 | **Facts verified centrally before writing**, per the round-3 D31 pattern: WHO's 2015 free-sugars guideline (10%/5% thresholds, 25g/6-tsp at 5%), a Cochrane systematic review of honey for acute cough (pub.5, 2018/2021), USDA FoodData Central's honey nutrition figures (64 kcal/tbsp, 17.2g sugar), the American Diabetes Association / Mayo Clinic position on honey and blood glucose, and Malaysian MOH sugar guidance (50g/10-tsp at 10%) — all in `round-5/FACTS-VERIFIED.md` with sources. No statute numbers, no invented RM figures, no SUBUR-specific dose or GI number anywhere in the round | assumed |
| D51 | 2026-08-26 | **A gate probe failed all 18 new articles AND a known-good round-4 control identically — 54 apparent failures, zero real ones.** A custom JSON-LD checker assumed a bare array of schema objects; this site emits one script with a top-level `@graph` array, and every article on the site (not just round 5's) uses that shape. Caught by running the same probe against `en/can-babies-have-honey` as a control before trusting the 54 failures — it failed the same way. Fixed the probe's `@graph` unwrapping, re-ran: PASS on all 18, zero content touched. See [[feedback_run_a_control_before_believing_a_probe]] | assumed |
| D52 | 2026-08-26 | **No new photography this round** — all 6 new figure keys reuse frames from the 2026-08-26 image-reuse round (`honey-in-glass`, `honey-dipper`, `spoon-close`, `board-and-nuts`, `blossom-and-light`, `table-setting`), chosen deliberately unbranded for the two most sensitive topics (diabetes, cough), mirroring the live pregnancy/allergy convention. This pushes several frames to 3-4 shares within one language; the assignment script prints every repeat and the standing shortfall (18 frames vs. now 63 total keys) is unchanged in kind from the prior round, just larger in degree | assumed |
| D53 | 2026-08-26 | **Round 6 starts.** Ships **6 articles per language × 3 languages = 18 articles**, single-session run, off `master` @ `6ce969a` (round 5's merge). Same constraint as every round: no keyword API, no GSC quota this session | approved (user instruction) |
| D54 | 2026-08-26 | **Six topics, all three-language groups (G53–G58)**: honey vitamins/minerals/antioxidants, is honey halal, honey before/after exercise, bulk honey gifts for events (B2B), how much honey for a toddler, honey and dental health. Every focus keyword checked against all 97 live articles (79 pre-round-5 + 18 from round 5) plus the other 17 in this round — no collision. Full map and cannibalisation check in `ROUND-6-MAP.md` | assumed |
| D55 | 2026-08-26 | **Refused before writing: "how to spot fake honey at home".** Overlaps the live `how-to-identify-pure-honey`/`cara-kenal-madu-asli`/`kayfa-tamiz-al-asal-al-asli` trio almost exactly — those three already debunk the water/flame/thumb tests in detail. Dropped in favour of the dental-health topic (G58) instead | assumed |
| D56 | 2026-08-26 | **The halal article (G54) mirrors, does not exceed, SUBUR's existing published position.** `llms/{en,ms,ar}.txt` already answer "Is SUBUR Honey halal?" with: composition (honey, comb, nuts, spices, all named), no alcohol, no animal ingredient beyond the honey, and no certificate number published — certification questions routed to Berkat Madinah on WhatsApp. The new article states the same three facts in the same order in all three languages, per the D37 fabrication-by-denial precedent: this is exactly the shape of claim that gets contradicted by an over-eager writer, so the SUBUR-specific paragraph was written first from the existing FAQ text, not drafted independently and checked after | assumed |
| D57 | 2026-08-26 | **G57 (toddler, 25g/6 tsp AHA) and G51 (round 5, adult, 25g/6 tsp WHO) land on the identical number from two different bodies for two different populations — stated as coincidence in both articles, in both directions**, to pre-empt the cross-language "two facts disagree" pattern that shipped in rounds 3 and 4 (D39, D43). Also caught during review: the Arabic toddler article's under-12-months citation first linked to the pregnancy article instead of the dedicated infant article (`hal-al-asal-masmuh-lil-atfal-aqal-min-sana`), which Arabic actually has — corrected before the gate ran, not after | assumed |
| D58 | 2026-08-26 | **Bulk/corporate gifting (G56) is built entirely on the site's own already-published wholesale-enquiry fact** (D37: a form on every locale's homepage, linked from `/contact/` and `/retail/`) and states explicitly, in a warning callout in all three languages, that no price, MOQ or lead time is published anywhere on the site — the weakest-fact-density topic in six rounds, and the shortest articles (666–771 words) as an honest consequence rather than padded with invented specifics | assumed |
| D59 | 2026-08-26 | **First gate run found a real defect, not a probe bug this time**: 8 of 18 articles (the ones with the heaviest inline disclaimers — exercise, bulk gifts, toddler, teeth) had meta descriptions over 165 characters, up to 216. Trimmed all 8, re-ran: PASS. The `@graph`-aware JSON-LD check built for round 5 was reused unmodified and needed no fix, which is itself evidence the round-5 control fix was correct rather than a one-off patch | assumed |
| D60 | 2026-08-26 | **Image reuse continues, deliberately spread onto the least-used frames.** All 6 new figure keys assigned to `lavender-and-ginger`, `jar-and-botanicals`, `couple-at-table`, `couple-sharing`, `jar-and-walnuts`, `ingredients-left` — the six lowest-count frames from rounds 4–5 — rather than adding further to `jar-alone` (7) or `honey-dipper` (6). Library is now 18 frames against **69** total figure keys across six rounds; the shoot recommendation in `docs/press/images/INVENTORY.md` is unchanged in kind and grows more urgent every round it isn't acted on | assumed |
